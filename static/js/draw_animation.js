const pool=JSON.parse(document.querySelector('#student-nos').textContent);const btn=document.querySelector('#draw-button'),roller=document.querySelector('#roller');btn.onclick=async()=>{btn.disabled=true;let elapsed=0,delay=40;const spin=()=>{roller.textContent=pool[Math.floor(Math.random()*pool.length)]||'--';elapsed+=delay;delay=Math.min(240,delay*1.08);if(elapsed<2200)setTimeout(spin,delay)};spin();const csrf=document.querySelector('[name=csrfmiddlewaretoken]').value;const res=await fetch(location.href,{method:'POST',headers:{'X-CSRFToken':csrf}}),data=await res.json();setTimeout(()=>{if(data.error){roller.textContent=data.error}else{roller.textContent=data.student_no;document.querySelector('#winner').textContent=data.name;document.querySelector('#student-id').value=data.id;document.querySelector('#score-form').hidden=false}btn.disabled=false},2600)};
